extends layout

block content
  h1= title 
  a(href=`/new`)= "Send a message"


  main 
    ul 
    each message in messages
      li 
        p= message.text 
        p= message.user
        p= message.added


